// import { forwardRef } from "react"


// // export default function JumpArticle(prevArticle, nextArticle) {
//     return(
//         <nav className="jumpSection">
//             {prevArticle!=null &&<button onClick={() => prevArticle.current?.scrollIntoView({behavior: 'smooth'})}>Previous Article</button>}
            
//             <div/>

//             {nextArticle!=null &&<button onClick={() => nextArticle.current?.scrollIntoView({behavior: 'smooth'})}>Next Article</button>}
//         </nav>
//     )

// }

// export default const JumpArticle = forwardRef